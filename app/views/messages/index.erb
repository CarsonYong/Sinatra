    <link href='http://fonts.googleapis.com/css?family=Lobster|Shadows+Into+Light|Open+Sans:400,700,400italic,700italic' rel='stylesheet' type='text/css'>
    <style>
      body {
        font-family: 'Lobster';
        background-color: #6ab47b;
        padding: 5px;
          }
    </style>

<% @messages.each do |message| %>
  <div class="message">
    <h3><a href="/messages/<%= message.id %>"><%= message.title %></a> <small>(by: <%= message.author %>)</small></h3>
    <p>
      <%= message.content %>
    </p>
  </div>
  <a href="/messages/new">Post a message... It's FREE (for now)!</a>
<% end %>